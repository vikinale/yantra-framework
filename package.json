{
  "name": "yantra-framework-docs",
  "private": true,
  "type": "module",
  "description": "Documentation site for the Yantra PHP framework (VitePress). Node is a docs-only dev dependency; the framework itself has zero runtime dependencies.",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
